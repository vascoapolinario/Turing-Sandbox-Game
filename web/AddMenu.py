# AddMenu is not available in the web version (no Workshop feature).
# This stub exists only to prevent ImportError if the file is ever loaded.

class AddMenu:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("Workshop / AddMenu is not available in the web version.")
